import styled from "styled-components";

export const ContainerRow = styled.div`
	display: flex;
	flex-direction: row;
	margin-bottom: 1.5vh;

    img {
        height: 5.5vh;
        width: auto;
        opacity: 1;
		margin-top: -1.5vh;
    }
`;